{
  "name": "easy-files",
  "version": "2.0.0",
  "private": true,
  "description": "EasyFiles — self-hosted обмен файлами с паролями, сроком хранения и админ-панелью",
  "license": "MIT",
  "scripts": {
    "install-all": "npm install --prefix server && npm install --prefix client",
    "build": "npm run build --prefix client",
    "start": "node scripts/ensure-client.js && node server/index.js",
    "server": "node server/index.js",
    "client": "npm run dev --prefix client",
    "dev": "node scripts/ensure-client.js && node server/index.js & npm run client"
  }
}
